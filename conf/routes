# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
#GET     /                           controllers.HomeController.index()

GET     /last-news/:n               controllers.NewsController.getNLastNews(n: Int)
GET     /new/:id                    controllers.NewsController.getNewById(id: String)
POST    /login                      controllers.LoginController.login(user: String, password: String)
GET     /create                     controllers.NewsController.createNews(title: String, author: String, content: String, description: String, eventImage: String)
### NoDocs ###
GET   /docs/swagger-ui/*file        controllers.Assets.at(path:String="/public/lib/swagger-ui", file:String)
### NoDocs ###
GET   /assets/*file                 controllers.Assets.versioned(path="/public", file: Asset)
